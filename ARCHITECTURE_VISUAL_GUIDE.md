# 🏗️ Complete Architecture - Feign Client & RabbitMQ

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           EDUKA MICROSERVICES PLATFORM                       │
│                                                                              │
│  ┌─────────────────┐                 ┌──────────────────┐                  │
│  │   Frontend      │                 │   API Gateway    │                  │
│  │   Angular       │────────────────▶│   (Port 8888)    │                  │
│  │  (Port 4200)    │   HTTP Requests │   + Keycloak     │                  │
│  └─────────────────┘                 └──────────────────┘                  │
│                                              │                               │
│                                              │                               │
│                        ┌─────────────────────┴─────────────────┐           │
│                        │     Eureka Server (Port 8761)          │           │
│                        │     Service Discovery & Registry       │           │
│                        └─────────────────────┬─────────────────┘           │
│                                              │                               │
│                 ┌────────────────────────────┼──────────────────────┐      │
│                 │                            │                      │       │
│         ┌───────▼────────┐          ┌───────▼────────┐    ┌───────▼──────┐│
│         │  Restaurant    │          │    Library     │    │   Housing    ││
│         │   Service      │          │    Service     │    │   Service    ││
│         │  (Port 8083)   │          │  (Port 8084)   │    │ (Port 8085)  ││
│         │                │          │                │    │              ││
│         │  [H2 Database] │          │  [H2 Database] │    │ [H2 Database]││
│         └───────┬────────┘          └───────┬────────┘    └───────┬──────┘│
│                 │                            │                     │        │
│                 │  Feign Client #1           │ Feign Client #2     │        │
│                 │  (Order Notification)      │ (Book Notification) │        │
│                 │                            │                     │        │
│                 │                            │  Feign Client #3    │        │
│                 │                            │  (Room Notification)│        │
│                 │                            │                     │        │
│                 └────────────────────────────┼─────────────────────┘        │
│                                              │                               │
│                                    ┌─────────▼──────────┐                   │
│                                    │   Notification     │                   │
│                                    │     Service        │                   │
│                                    │   (Port 8086)      │                   │
│                                    │                    │                   │
│                                    │  POST /api/        │                   │
│                                    │  notifications/    │                   │
│                                    │  send              │                   │
│                                    └─────────┬──────────┘                   │
│                                              │                               │
│                                              │ RabbitTemplate               │
│                                              │ convertAndSend()             │
│                                              ▼                               │
│                          ┌───────────────────────────────────┐             │
│                          │      RabbitMQ Message Broker      │             │
│                          │         (Port 5672/15672)         │             │
│                          │                                   │             │
│                          │  ┌────────────────────────────┐  │             │
│                          │  │ notification.exchange      │  │             │
│                          │  │   (TopicExchange)          │  │             │
│                          │  └──────────┬─────────────────┘  │             │
│                          │             │                     │             │
│                          │   ┌─────────┴─────────┐          │             │
│                          │   │                   │          │             │
│                          │   ▼                   ▼          │             │
│                          │ ┌─────────┐       ┌─────────┐   │             │
│                          │ │ ORDER   │       │ LIBRARY │   │             │
│                          │ │ Queue   │       │ Queue   │   │             │
│                          │ └─────────┘       └─────────┘   │             │
│                          │   ▼                   ▼          │             │
│                          │ ┌─────────┐       ┌─────────┐   │             │
│                          │ │ HOUSING │       │  EMAIL  │   │             │
│                          │ │ Queue   │       │  Queue  │   │             │
│                          │ └─────────┘       └─────────┘   │             │
│                          └────────┬──────────────┬──────────┘             │
│                                   │              │                         │
│                                   │ @RabbitListener                        │
│                                   │                                        │
│                          ┌────────▼──────────────▼──────────┐             │
│                          │   NotificationListener           │             │
│                          │                                  │             │
│                          │  📦 handleOrderNotification()    │             │
│                          │  📚 handleLibraryNotification()  │             │
│                          │  🏠 handleHousingNotification()  │             │
│                          │  ✉️  handleEmailNotification()    │             │
│                          └──────────────────────────────────┘             │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Feign Client Communication Flow

### Scenario #1: Order Notification Flow
```
User Places Order
    │
    ▼
┌───────────────────────┐
│ Frontend (Angular)    │
│ POST /api/orders      │
└───────┬───────────────┘
        │
        ▼
┌───────────────────────┐
│ API Gateway           │
│ Route to Restaurant   │
└───────┬───────────────┘
        │
        ▼
┌───────────────────────────────┐
│ Restaurant Service            │
│ OrderService.createOrder()    │
│                               │
│  1. Save order to H2 DB       │
│  2. Calculate total           │
│  3. Set status = PENDING      │
│                               │
│  ┌─────────────────────────┐ │
│  │ sendOrderNotification() │ │
│  │ (Feign Client Call)     │ │
│  └────────┬────────────────┘ │
└───────────┼───────────────────┘
            │
            │ 🔥 FEIGN CLIENT COMMUNICATION #1
            │
            ▼
┌───────────────────────────────┐
│ Notification Service          │
│ POST /api/notifications/send  │
│                               │
│  NotificationController       │
│        ↓                      │
│  NotificationService          │
│        ↓                      │
│  rabbitTemplate.              │
│  convertAndSend(              │
│    "notification.exchange",   │
│    "notification.order",      │
│    message                    │
│  )                            │
└───────────┬───────────────────┘
            │
            ▼
┌───────────────────────────────┐
│ RabbitMQ                      │
│ ORDER Queue                   │
└───────────┬───────────────────┘
            │
            ▼
┌───────────────────────────────┐
│ NotificationListener          │
│ @RabbitListener               │
│                               │
│ handleOrderNotification()     │
│   ↓                           │
│ Log: "📦 Received ORDER       │
│       notification"           │
│   ↓                           │
│ Send Email/SMS/Push           │
└───────────────────────────────┘
```

### Scenario #2: Book Borrow Notification Flow
```
Student Borrows Book
    │
    ▼
┌───────────────────────────────┐
│ Library Service               │
│ BookService.borrowBook()      │
│                               │
│  1. Check availability        │
│  2. Decrease available copies │
│  3. Save to H2 DB             │
│                               │
│  ┌────────────────────────┐  │
│  │ sendBookBorrow         │  │
│  │ Notification()         │  │
│  │ (Feign Client Call)    │  │
│  └────────┬───────────────┘  │
└───────────┼───────────────────┘
            │
            │ 🔥 FEIGN CLIENT COMMUNICATION #2
            │
            ▼
┌───────────────────────────────┐
│ Notification Service          │
│ Route to LIBRARY queue        │
└───────────┬───────────────────┘
            │
            ▼
┌───────────────────────────────┐
│ RabbitMQ LIBRARY Queue        │
└───────────┬───────────────────┘
            │
            ▼
┌───────────────────────────────┐
│ NotificationListener          │
│ handleLibraryNotification()   │
│                               │
│ Log: "📚 Book borrowed:       │
│       {bookTitle}"            │
└───────────────────────────────┘
```

### Scenario #3: Room Reservation Notification Flow
```
Student Reserves Room
    │
    ▼
┌───────────────────────────────┐
│ Housing Service               │
│ RoomService.reserveRoom()     │
│                               │
│  1. Check availability        │
│  2. Set isAvailable = false   │
│  3. Save to H2 DB             │
│                               │
│  ┌────────────────────────┐  │
│  │ sendRoomReservation    │  │
│  │ Notification()         │  │
│  │ (Feign Client Call)    │  │
│  └────────┬───────────────┘  │
└───────────┼───────────────────┘
            │
            │ 🔥 FEIGN CLIENT COMMUNICATION #3
            │
            ▼
┌───────────────────────────────┐
│ Notification Service          │
│ Route to HOUSING queue        │
└───────────┬───────────────────┘
            │
            ▼
┌───────────────────────────────┐
│ RabbitMQ HOUSING Queue        │
└───────────┬───────────────────┘
            │
            ▼
┌───────────────────────────────┐
│ NotificationListener          │
│ handleHousingNotification()   │
│                               │
│ Log: "🏠 Room reserved:       │
│       {roomNumber}"           │
└───────────────────────────────┘
```

---

## Docker Container Architecture

```
┌────────────────────────────────────────────────────────────────┐
│                    Docker Network: eduka-network                │
├────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │  Keycloak    │  │   Eureka     │  │  RabbitMQ    │        │
│  │  :8080       │  │   :8761      │  │  :5672       │        │
│  │              │  │              │  │  :15672 (UI) │        │
│  └──────────────┘  └──────────────┘  └──────────────┘        │
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │ API Gateway  │  │ Restaurant   │  │  Library     │        │
│  │  :8888       │  │  Service     │  │  Service     │        │
│  │              │  │  :8083       │  │  :8084       │        │
│  └──────────────┘  └──────────────┘  └──────────────┘        │
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │  Housing     │  │ Notification │  │   Frontend   │        │
│  │  Service     │  │  Service     │  │   (Nginx)    │        │
│  │  :8085       │  │  :8086       │  │   :4200      │        │
│  └──────────────┘  └──────────────┘  └──────────────┘        │
│                                                                 │
└────────────────────────────────────────────────────────────────┘

Total Containers: 9
- 3 Infrastructure (Keycloak, Eureka, RabbitMQ)
- 5 Microservices (Gateway, Restaurant, Library, Housing, Notification)
- 1 Frontend (Angular)
```

---

## Service Ports Reference

| Service | Port | Purpose | Status |
|---------|------|---------|--------|
| Frontend | 4200 | Angular UI | ✅ Running |
| Keycloak | 8080 | OAuth2 Authentication | ✅ Running |
| Eureka | 8761 | Service Discovery | ✅ Running |
| Restaurant | 8083 | Restaurant Management | ✅ Running |
| Library | 8084 | Library Management | 🔄 Enhanced |
| Housing | 8085 | Housing Management | 🔄 Enhanced |
| Notification | 8086 | Notification Service | 🆕 New |
| API Gateway | 8888 | API Gateway | ✅ Running |
| RabbitMQ AMQP | 5672 | Message Broker | 🆕 New |
| RabbitMQ UI | 15672 | Management Console | 🆕 New |

---

## Technology Stack

```
┌─────────────────────────────────────────────┐
│           Frontend Layer                     │
├─────────────────────────────────────────────┤
│  • Angular 18                                │
│  • TypeScript                                │
│  • Bootstrap 5                               │
│  • HTTP Client (REST calls)                  │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│         API Gateway Layer                    │
├─────────────────────────────────────────────┤
│  • Spring Cloud Gateway                      │
│  • Keycloak OAuth2                           │
│  • JWT Token Validation                      │
│  • Route Filtering                           │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│      Service Discovery Layer                 │
├─────────────────────────────────────────────┤
│  • Netflix Eureka Server                     │
│  • Service Registry                          │
│  • Load Balancing                            │
│  • Health Checks                             │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│        Microservices Layer                   │
├─────────────────────────────────────────────┤
│  • Spring Boot 3.1.5                         │
│  • Spring Data JPA                           │
│  • H2 In-Memory Databases                    │
│  • OpenFeign (Synchronous Communication)     │
│  • Spring AMQP (Asynchronous Communication)  │
│  • Lombok                                    │
│  • Jakarta Validation                        │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│      Messaging Layer (NEW)                   │
├─────────────────────────────────────────────┤
│  • RabbitMQ 3 (Message Broker)               │
│  • TopicExchange                             │
│  • 4 Queues (ORDER, LIBRARY, HOUSING, EMAIL) │
│  • JSON Message Converter                    │
│  • @RabbitListener                           │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│      Containerization Layer                  │
├─────────────────────────────────────────────┤
│  • Docker                                    │
│  • Docker Compose                            │
│  • Multi-stage Builds                        │
│  • Health Checks                             │
│  • Volume Persistence                        │
└─────────────────────────────────────────────┘
```

---

## Communication Patterns Summary

### 1. Client ↔ Frontend
- **Pattern**: HTTP/HTTPS
- **Protocol**: REST
- **Example**: User clicks "Place Order" button

### 2. Frontend ↔ API Gateway
- **Pattern**: HTTP with JWT
- **Protocol**: REST
- **Example**: Angular HTTP Client → Gateway

### 3. Gateway ↔ Microservices
- **Pattern**: HTTP via Eureka
- **Protocol**: REST with Service Discovery
- **Example**: Gateway routes to restaurant-service

### 4. Microservice ↔ Microservice (Feign)
- **Pattern**: Synchronous REST
- **Protocol**: HTTP
- **Example**: Restaurant → Notification Service

### 5. Microservice ↔ RabbitMQ
- **Pattern**: Asynchronous Messaging
- **Protocol**: AMQP
- **Example**: Notification Service → ORDER Queue

### 6. RabbitMQ ↔ Listener
- **Pattern**: Event-Driven
- **Protocol**: AMQP with @RabbitListener
- **Example**: ORDER Queue → NotificationListener

---

## Data Flow Example: Complete Order Journey

```
1. User Action
   👤 User clicks "Place Order" on frontend
   
2. Frontend Processing
   🖥️  Angular sends POST /api/orders with order data
   
3. Gateway Processing
   🚪 API Gateway validates JWT token
   🚪 Routes request to restaurant-service via Eureka
   
4. Restaurant Service Processing
   🍽️  OrderService.createOrder() receives request
   🍽️  Validates order data
   🍽️  Calculates total amount
   🍽️  Saves order to H2 database
   🍽️  Order saved with ID = 123
   
5. Feign Client Call (Synchronous)
   📡 OrderService.sendOrderNotification(order)
   📡 Feign Client calls POST /api/notifications/send
   📡 Notification Service receives request
   
6. RabbitMQ Routing (Asynchronous)
   🐰 NotificationService.sendNotification()
   🐰 RabbitTemplate sends to notification.exchange
   🐰 Routing key: notification.order
   🐰 Message routed to ORDER Queue
   
7. Listener Processing
   📨 NotificationListener.handleOrderNotification()
   📨 Processes message from ORDER Queue
   📨 Logs: "📦 Received ORDER notification"
   📨 Sends email/SMS/push notification
   
8. User Notification
   ✉️  User receives email confirmation
   📱 Push notification on mobile
   
TOTAL TIME: < 2 seconds
```

---

## Monitoring & Debugging

### Eureka Dashboard
```
URL: http://localhost:8761
View: All registered services and their instances
Check: Health status, uptime, replicas
```

### RabbitMQ Management UI
```
URL: http://localhost:15672
Login: guest/guest
View: Queues, exchanges, bindings, message rates
Check: Queue depths, message throughput, consumers
```

### Service Logs
```powershell
# All logs
docker-compose logs -f

# Specific service
docker logs restaurant-service-eduka -f

# Search for Feign calls
docker logs restaurant-service-eduka | Select-String "Feign"

# Search for notifications
docker logs notification-service-eduka | Select-String "📦|📚|🏠|✉️"
```

---

**Created**: January 2025  
**Status**: ✅ Implementation Complete  
**Next**: Docker Build & Testing
